<%= form_for(recruiter) do |f| %>
  <% if recruiter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recruiter.errors.count, "error") %> prohibited this recruiter from being saved:</h2>

      <ul>
      <% recruiter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :agency_id %>
    <%= f.number_field :agency_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :surname %>
    <%= f.text_field :surname %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :pass_active %>
    <%= f.check_box :pass_active %>
  </div>

  <div class="field">
    <%= f.label :acc_active %>
    <%= f.check_box :acc_active %>
  </div>

  <div class="field">
    <%= f.label :last_in %>
    <%= f.datetime_select :last_in %>
  </div>

  <div class="field">
    <%= f.label :password_digest %>
    <%= f.text_field :password_digest %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
